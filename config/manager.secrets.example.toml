# Copy to config/manager.secrets.toml and chmod 600.
# Never commit the private file.

[web]
password = "replace-with-a-strong-password"
session_secret = "replace-with-at-least-32-random-characters"

[steam]
password = ""
